# vim: filetype=gitconfig:
[engineSubject "core/config"]
	configPipeline = fromPipeline
	configSubject  = fromPipeline
	configEnv      = fromPipeline
	verifyHomePath = content/mk

[engineEnv "tests"]
	verifyEnvPath  = engine.conf
