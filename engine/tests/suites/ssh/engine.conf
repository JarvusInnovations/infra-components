# vim: filetype=gitconfig
[engineEnv "tests"]
	sshConfigPath     =
	sshConfigArtifact = connect-artifact

[engineArtifact "connect-artifact"]
	path              = artifact_ssh_config
	producer          = connect-artifact-setup
