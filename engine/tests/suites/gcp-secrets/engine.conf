# vim: filetype=gitconfig:
[engineArtifact "tests-gcp-secrets"]
	path          = tests-gcp-secrets.txt
	producer      = gcp-secrets
	publisherItem = gcp-secrets
	gcpSecretID   = engine-tests-gcp-secrets

[engineEnv "tests"]
	gcpProject  =
